# Do not commit a populated file. chmod 600. No shell expansion is performed.
# Mint in Jevi Ops for one registered worker with permission_profile=research_worker.
JEVI_WORKER_TOKEN=
# Independent model provider key. The worker token is never passed to Hermes.
HERMES_MODEL_KEY=
