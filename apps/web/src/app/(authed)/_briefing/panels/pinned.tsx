import type { BriefingContext } from '../registry';

// Pinned — anything the user pinned, in their order. Placeholder until the
// pins feature commit lands; registered now so the panel order is complete.

export function PinnedPanel(_props: { ctx: BriefingContext }) {
  return null;
}
