import type { BriefingContext } from '../registry';

// Health — snapshot from /api/health/overview. Placeholder until the health
// panel commit lands; registered now so the panel order is complete.

export function HealthPanel(_props: { ctx: BriefingContext }) {
  return null;
}
